### *Д*ерево *О*трезков

![[ДО.canvas]]
Красный - корень
Синий - листья

Работает для:
- $a(bc) = (ab)c$
- $RMQ$ операция и ее количество на отрезке
- Длину максимального подотрезка из нулей
- Объединение прямоугольников


---
### Количество точек в прямоугольнике

![[Сканлайн.canvas]]
Решение:
1. Будем использовать относительные координаты (то есть где начинается/заканчивается прямоугольник)

``` C++ Pseudo
struct event {
	int x;
	int type = 0;
	int index;
	int yl, yr;
};

bool cmp(const event &lhs, const event &rhs) {
	return {lhs.x, lhs.type} < {rhs.x, rhs.type};
}

//Основной код

vector<int> xs; //Все x-координаты
unordered_map<int, int> compXS;
sort(xs.begin(), xs.end());
xs.resize(unique(xs.begin(), xs.end()) - xs.begin)());
for (int i = 0; i < xs.size(); i++) compXS[xs[i]] = 1;
// Заменяем все координаты на сжатые

vector<event> events;
sort(events.begin(), events.end());

for (auto e : events) {
	if (e.type == 0) 
		tree.upd(e.yr, e.yl, 1);
	if (e.type == 1) 
		ans[e.index] == tree.get(e.yr, e.yl + 1);
}

// Разжимаем и выводим ответ
```